import { OnGatewayConnection, OnGatewayDisconnect } from "@nestjs/websockets";
import { Server, Socket } from "socket.io";
export declare class gameSocket implements OnGatewayConnection, OnGatewayDisconnect {
    server: Server;
    numberOfGame: number;
    x: number;
    y: number;
    oneHost: [string, boolean, boolean, boolean, boolean][];
    oneWaiting: Socket[];
    twoHost: [Socket, string, boolean, boolean, boolean, boolean][];
    twoWaiting: Socket[];
    threeHost: [Socket, string, boolean, boolean, boolean, boolean][];
    threeWaiting: Socket[];
    XVelocityMin1: number;
    XVelocityMax1: number;
    XVelocityMin2: number;
    XVelocityMax2: number;
    YvelocityMin: number;
    YvelocityMax: number;
    handleConnection(): void;
    handleDisconnect(): void;
    handleMovement(client: Socket, newpos: number): void;
    handlePoint(client: Socket, which: number): void;
    handleUpdate(client: Socket, data: any): void;
    handleMulti(client: Socket, data: any): void;
    HandleNewPower(client: Socket, data: any): void;
    handleRandom(client: Socket, data: any): void;
    handlePower(client: Socket, data: any): void;
    handle1v1(client: Socket, data: any): void;
    handle2v2(client: Socket, data: any): void;
}
